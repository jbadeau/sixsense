# kafka-ui 
https://github.com/provectus/kafka-ui

1. `helm repo add kafka-ui https://provectus.github.io/kafka-ui`
2. update values.yaml to point to cluster
3. `helm install --values ../values.yaml kafka-ui .`
