apiVersion: builtin
kind: PatchTransformer
metadata:
  name: telegraf-deployment
target:
  kind: Deployment
  name: telegraf
patch: |-
  - op: add
    path: /spec/template/spec/containers/0/envFrom
    value:
      - secretRef:
          name: influxdb